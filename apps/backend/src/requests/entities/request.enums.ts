export enum RequestStatus {
    OPEN = 'Open',
    IN_PROGRESS = 'In Progress',
    ON_HOLD = 'On Hold',
    CLOSED = 'Closed',
}

export enum ProjectStatusByRequest {
    NOT_STARTED = 'Not Started',
    ACTIVE = 'Active',
    FINISHED = 'Finished',
}